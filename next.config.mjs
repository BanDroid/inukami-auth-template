/** @type {import('next').NextConfig} */
const nextConfig = {
  // if using google cloudshell, use https://$PORT-$WEB_HOST, else undefined
  // assetPrefix: process.env.WEB_HOST
  //   ? `https://${process.env.PORT}-${process.env.WEB_HOST}`
  //   : undefined,
};

export default nextConfig;
